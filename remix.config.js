// @ts-check

/**
 * @type {import('@remix-run/dev').AppConfig}
 */
const config = {
  appDirectory: 'src/app',
  future: {
    v2_routeConvention: true,
    v2_meta: true,
  },
}

module.exports = config
